# OKR Lens — 통합 MVP Spec (최종 v2.0)

> 최종본 v2.0 | 2026-06-24 | 작성: 신승희 / 인사노무팀 + Claude Code 피드백 통합
> 원본: 피평가자용 spec(R1) · 평가자용 spec v1.0(R2) · 인사담당자용 spec v1.6(R3) 통합
> 시연 스토리: **피평가자 → 평가자 → 인사담당자** (인사담당자 클로징)
> 
> **B모드 개선 완료**: 아키텍처 명시 + 3주 일정 재구성 + 팀 협력 가이드 추가

---

## 0. 개요 · 통합 · 시연 연계 (3역할 공통)

**제품 한 줄**: 2026 OKR 기반 개인평가제도를 처음 운영하는 IT 조직을 돕는 3역할 도구 — KR을 잘 쓰게(피평가자) → 잘 검토·코칭·승인하게(평가자) → 전사 품질·위험을 보게(인사담당자).

**3역할 / 동시 개발**: R1 피평가자 · R2 평가자 · R3 인사담당자를 **담당자 1인씩, 3명이 동시 개발**.

**시연 스토리 (핸드오프)**:
1. **R1 피평가자** — 업무·제약·협업(상위 Objective는 선택) 입력 → AI가 KR 초안 4종 생성 → 대화형으로 정제 → (멀티)AI 교차 검증 → 확정·제출.
2. **R2 평가자** — 제출된 KR을 받아 룰 위반 자동 점검(AI Validation)·코칭 후 승인.
3. **R3 인사담당자 (클로징)** — 승인된 KR 포함 전사 KR을 집계 → 이의제기 위험 KR 사전 식별 → 업무군 도출·확정 → 구성·난이도/품질 분포 제시. "한 사람의 KR이 전사 그림의 일부가 되는" 마무리.
   - 1·2단계에서 등장한 KR을 **공유 mock 전사 데이터셋에 포함**시켜 R3 화면에서 다시 보이게 한다 → 스토리 연결.

**공유 KR 데이터 계약** (3역할 공통 스키마, 캘리브레이션 데이터 컬럼과 정합):
```
{ id, 부서, 담당자, 직급,
  objective, kr, 측정방법, 달성도: {S,A,B,C,D}, 난이도,
  체크: [①..⑨],                         // 난이도 체크리스트
  평가자, 승인상태,                        // R2가 갱신 (제출/승인/반려)
  업무군, 경계, 위험태그:[], 위험도 }       // 진단 엔진 1차 부여 → R3가 업무군 확정
```
R1 생성·제출 → R2 검토·승인(승인상태 갱신) → R3 읽기·업무군 확정. **각 역할은 자기 화면만 구현**하고, 다른 역할과는 이 데이터로만 연계.

**공유 진단 엔진**: 위험 태깅 + 업무군 1차 분류 룰 모듈은 **R2(승인 시 위반 점검)와 R3(전사 위험 식별)가 공유** — 판정 일관·중복 구현 방지. (캘리브레이션 analyze.py 규칙 이식)

**구동 환경**: **로컬 서버(localhost) 구동**을 기본(AXHub 사용 불가 가능성 대비). 스택을 **Next.js로 통일**했으므로 통합 방식은 **단일 Next.js 앱에 역할별 라우팅**(`/employee`·`/evaluator`·`/hr`)으로 한다 — 한 번의 `npm run dev`로 3역할을 모두 띄워 시연 핸드오프가 자연스럽다. 모든 데이터는 로컬 보관·외부 전송 없음. AXHub 가용 시 코드 변경 최소로 사내 빌드 이관.

---

## 1. 문제 — 누구의 어떤 불편을 푸는가

**R1 피평가자**: OKR 운영 경험은 있으나 KR 문장을 직접 쓰는 자신감이 부족하다. 무엇을 입력해 시작할지, 자기 KR이 측정 가능한지·도전적인지 스스로 판단하기 어렵고, 고치려 해도 어떻게 다듬을지 막막하며, 매번 동료·평가자에게 묻기는 부담스럽다.

**R2 평가자(팀장)**: OKR 작성 기준(운영/전략혁신, 난이도 체크 9항목, KR 4~6개 등)이 복잡해 승인 시 룰 위반을 즉각 판단하기 어렵다. 월 1회 1on1·분기 Align 기록을 의무 등록해야 하나 무엇을 남길지 막막하다. 연말에 팀원별 KR 달성도·코칭 이력을 종합 판단하기 어렵다.

**R3 인사담당자**: 전 직원 KR을 일관 기준으로 검토·코칭해야 하나 인력은 1명이라 형식적 검토에 그친다. 평가위원회 전 이의제기 위험 KR을 미리 거를 도구가 없다. 전사·부서 지표가 어떤 업무 성격(매뉴얼·자동화·개발 등)에 몰렸는지, 측정가능성·도전성·난이도가 어떻게 분포하는지 한 화면에서 못 본다.

---

## 2. 사용자 — 핵심 페르소나

| 역할 | 페르소나 | 상황·목표 |
|------|----------|-----------|
| **R1 피평가자** | 자기 KR을 작성하는 직원 (예: HR기획팀 작성자) | 입력만 하면 초안을 받고, 품질 점수·대화형 정제로 측정 가능·도전·정렬된 KR로 다듬어 평가자에게 자신 있게 제출하고 싶음 |
| **R2 평가자** | 박준혁 (팀장, 책임급, IT서비스개발팀 5~8명) | 첫 OKR 제도. "승인을 잘못했나", "코칭 기록을 안 남기면 불리한가" 두려움. 공정·근거 있게 마무리하고 싶음 |
| **R3 인사담당자** | 신승희 (인사노무팀, 과장급, 제도 단독 설계·운영) | 평가위원회 전 위험 KR 사전 식별·코칭, 업무군 구성·난이도 분포로 평가자에게 참고자료 제공 |

---

## 3. 핵심 기능 (MVP) — 역할별 2~3일 범위

### R1 피평가자 — AI KR 작성·정제 Copilot (단계형 위저드)

작성자가 KR을 처음부터 끝까지 만들고 다듬어 평가자에게 제출하는 흐름. 화면 단계: **① 목표 선택 → ② KR 초안 입력 → ③ 초안 확인 → ④ 대화형 정제 → ⑤ 최종 검토 → ⑥ 완료(제출)**.

**Feature 1. KR 초안 입력 → AI 4종 초안 생성**
- 업무 설명 · 제약 조건 · 협업 대상을 입력하면(상위 Objective는 선택 입력) AI가 현실적인 KR 초안 **4가지**를 생성한다.

**Feature 2. KR 대화형 정제**
- "데이터 품질 기준을 더 구체적으로", "목표를 90점으로 올려 더 도전적으로" 같은 **자연어 피드백** → AI가 수정안 + 개선 포인트 제시, "다른 제안 보기 / 이 내용으로 적용".

**Feature 3. 최종 검토 — (멀티) AI 교차 검증**
- 정제된 KR을 강점·보완·평가위원회 리스크(측정/과도한 목표/외부 의존성)·추천 수정안으로 교차 검토하고, **AI 종합 추천안 + 버전 히스토리**를 제시. 항목별 "이 수정안 반영 / 일부 인용 / 무시".
- ※ **멀티 모델(Claude·Gemini·OpenAI) 동시 교차 검증은 외부 다중 API가 필요해 폐쇄망·2~3일 MVP 범위 밖.** MVP는 **단일 모델(Claude) 또는 룰 기반 단일 검토**로 구현하고, 멀티 AI는 인터넷 가용 시연/Phase 2 확장으로 둔다.

**Feature 4. 확정 → 평가자 제출**
- 최종 KR을 편집·확정하면 공유 KR 데이터(승인상태=제출)로 **평가자(R2)에게 넘어간다** → 시연 핸드오프의 시작점.

> MVP 핵심(2~3일): Feature 1·2 + 4. Feature 3은 단일 모델/룰 기반으로 축소 구현.

### R2 평가자 — OKR Lens

**Feature 1. 메인 대시보드**
- 이번 달 코칭 일정 캘린더 (예정 미팅 D-day 표시).
- 이번 달 현황 요약 카드 3개 (OKR 승인률 / 코칭 등록률 / 평가 완료율).

  **※ 이번 시연 제외**: 팀원 실시간 랭킹 테이블 · 이모지 등급(🏆/🟢/🟡/🟠/🚨) · 최하 팀원 코칭 미등록 알림 배지(14일 초과 등) · 대시보드 내 랭킹 공통 노출 (고도화 단계에서 추가)

**Feature 2. AI 코칭 지원**
- 팀원별 KR 달성 현황 카드 조회.
- 코칭 기록 수기 입력 + **AI 제안 버튼**(Claude API) — 클릭 시 기존 내용 덮어쓰기 Confirm 팝업 → 확인 시 대체.
- 1on1/Align 미팅 일정 등록 및 이력 조회.
- 코칭 메모 AI 배지(🤖 AI 자동입력 / ✏️ 수정됨).

  **※ 이번 시연 제외**: 코칭 스케줄 팝업·AI 코칭 상단에 랭킹 공통 노출 (고도화 단계에서 추가)

**Feature 3. OKR 검토·승인**
- 팀원 OKR 아코디언 뷰(Objective → KR → Initiative).
- **AI Validation 버튼**: 2026 제도 기준 위반 자동 감지 (KR 개수 4~6개, 난이도 상 비율 50% 이내, 수치 측정 가능 여부 등).
- 승인 / 반려(사유 입력), OKR 수정 요청 승인 루프(수정 이력 보관).

### R3 인사담당자 — 전사 OKR 품질/구성 대시보드

**Feature 1. 이의제기 위험 KR 자동 식별 → 사전 코칭 리스트 (최우선)**
- 전 직원 KR 로드 시 룰 엔진이 KR별 **업무군**과 **이의제기 위험 유형·위험도**를 자동 태깅.
- 위험 유형(캘리브레이션 진단 체계와 동일): 공모형 *(선제안 등 전사 공통지표는 제외)* · 통제 불가 외생변수(SLA·만족도·매출·납기) · 지표 사용불가(자격·교육 둔갑) · 측정 모호 · 도전성 저하(유지형) · 단순 건수형 · 난이도 ④⑥의존(⑦⑧ 없이 신기술·전사만으로 "상").
- 위험도(상/중/하)·유형·부서 필터·정렬한 "사전 코칭 대상" 리스트 + 권고 1줄. KR 클릭 시 상세 패널(원문 + 위험 유형 + 체크①~⑨ + 왜 위험·권고).
- **위험 태그 = "코칭 후보(이상 징후 트리거)", 위반 아님** — 확정 아님, 사전 점검·보완용.

**Feature 2. 업무군 도출·확정 → 지표 구성 분석 (필수, 평가자 참고자료)**
- 업무군은 고정 목록이 아니라 **모인 지표를 분석해 도출·확정**한다.
  1. 1차 도출 — 엔진이 KR·측정·A등급 산출물을 분석해 분류. **시드 9종**(개발/요건·데이터/정합성·자동화·성능/튜닝·장애/운영안정·정시/납기/처리율·매뉴얼/표준/문서·보안/권한/개인정보·고객/사업기여/제안, *향후 확장 가능*)에서 출발, 안 맞는 지표는 '경계·미분류'로 모음.
  2. 검토·확정 — 인사담당자가 업무군 추가·이름변경·병합 + 경계/미분류 재지정 후 **"확정"**. 확정 분류가 분석 기준.
- 확정 분류로 전사·부서·팀별 **업무군별 지표 건수·비율** 시각화 → "매뉴얼이 많은지, 자동화가 많은지, 개발 중심인지" 한눈에. 부서 간 구성 비교.
- **목적**: 평가자 판단용 참고자료(구성 정보). 난이도 일관성 검증 아님(같은 업무군이라도 시스템·환경에 따라 난이도는 정당하게 달라짐).

**Feature 3. 난이도·품질 분포 대시보드 (업무군과 별개 차원) — 선택**
- 난이도 상/중/하 분포(전사·부서별, 업무군과 교차하지 않음).
- 품질 2축 분포 — **측정가능성·도전성**(양호/주의/위험), 부서 편차. *(정렬성 제외 — 상위 Objective 선택 입력·팀 OKR 미정의, 4절)*
- 요약 카드(전사 KR 수 / 위험 KR 수·비율 / 코칭 대상 수 / 경계·미분류 수), 차트 클릭 시 드릴다운.
- **시간 부족 시 선택 사항**: mock 차트로 UI 프로토타입만 표시 가능

---

## 4. 제외 범위 — 이번엔 안 하는 것

**공통**
- 사용자 인증·권한 관리 (프로토타입 단일 계정 고정)
- 실제 DB·인사시스템/ERP 연동 (JSON/엑셀 mock 대체), 전년 데이터 자동 축적
- 이메일/슬랙 알림 (앱 내 배지·리스트로 대체)
- 다른 역할 화면 자체 구현 (각 담당 몫, 공유 KR 데이터로만 연계)

**R1 피평가자**
- **품질 진단(실시간 3축 점수: 측정가능성·도전성·통제가능성) 화면 — 이번 시연 제외.** KR 초안 생성·대화형 정제·교차 검증에 집중
- **멀티 AI(Claude·Gemini·OpenAI) 동시 교차 검증 — 외부 다중 API 필요, 폐쇄망·MVP 범위 밖.** MVP는 단일 모델/룰 기반 단일 검토, 멀티 AI는 Phase 2(또는 인터넷 가용 시연)
- 작성 이력 영구 저장·버전 관리 (MVP는 세션 내 버전만)

**R2 평가자**
- **⛔ 팀원 랭킹 관련 항목** (실시간 랭킹 테이블 · 이모지 등급(🏆/🟢/🟡/🟠/🚨) · 최하 팀원 코칭 미등록 알림 배지 · 랭킹 공통 노출) — 이번 시연 제외, Feature 1·2에서 명시. 고도화 단계에서 추가
- 연말 평가 점수 입력 화면, 팀원 평가 이력 조회, 다면/상향평가, 이의제기 처리

**R3 인사담당자**
- **정렬성(상위 목표와의 정렬) 평가 — 스펙 전체 제외(R1 품질 진단·R3 분포 공통). 상위 Objective가 선택 입력이라 정렬 대상이 일정치 않고, 팀(상위) OKR도 미정의. 상위 Objective 상시화/팀 OKR 도입 시 Phase 2**
- 같은 업무군 내 난이도 일관성 자동 검증·난이도 자동 재산정 (시스템·환경 차이로 정당 → 위반 판정 안 함)
- 업무군 완전 비지도 자동 군집(임베딩) — MVP는 시드+경계 노출→확정 방식, Phase 2
- 연말 점수 입력·평가위원회 운영·이의제기 처리 워크플로우

---

## 5. 성공 기준 — 시연에서 무엇이 되면 성공인가

**R1 피평가자**
- [ ] 업무·제약·협업을 입력하면(상위 Objective는 선택) KR 초안 4종이 생성된다
- [ ] 자연어 피드백("더 도전적으로" 등)으로 KR이 정제된다(수정안 + 개선 포인트 제시)
- [ ] 확정하면 KR이 평가자에게 제출된다(공유 데이터 승인상태=제출)

**R2 평가자**
- [ ] OKR 카드에서 AI Validation 버튼을 누르면 위반 항목이 텍스트로 출력된다
- [ ] 코칭 AI 제안 버튼 → Claude 생성 → Confirm 팝업 후 텍스트 대체된다
- [ ] 승인/반려 동작이 UI에서 작동하고 상태가 변경된다
- [ ] 캘린더에 이번 달 1on1 일정이 표시된다 *(보너스)*

**R3 인사담당자**
- [ ] 전사 KR(공유 mock)을 로드하면 KR별 업무군(1차)·위험 유형이 자동 태깅된다
- [ ] 모인 지표로 업무군이 도출되고, 인사담당자가 추가·재지정 후 "확정"할 수 있다(경계/미분류 포함)
- [ ] 사전 코칭 대상 리스트가 위험도·유형·부서와 함께 출력·필터된다
- [ ] 확정 업무군 기준으로 부서별 구성(건수·비율)이 보인다
- [ ] 난이도 상/중/하 분포와 측정가능성·도전성 분포가 표시된다 *(선택)*
- [ ] 공통지표(선제안 등)는 공모형으로 오탐되지 않는다

**통합 (핸드오프)**
- [ ] R1이 작성·제출 → R2가 승인한 KR이 R3 전사 코칭 리스트·분포에 반영되어 나타난다

---

## 6. 기술 스택 & 아키텍처

**통합 권장**: 로컬 서버(localhost) 구동, 데이터 로컬 보관. **3역할 모두 Next.js로 통일** — 단일 앱에 역할별 라우팅으로 통합한다. 핵심 진단은 외부 API 없이 동작 → 로컬·폐쇄망 무관.

### 6.1 프로젝트 폴더 구조 (병렬 개발·git 충돌 최소화)

```
okr-lens/
├── .env.local                    (공유: API 키)
├── package.json                  (공유: 의존성)
├── next.config.js                (공유)
│
├── r1-employee/                  👈 R1 담당 (완전 독립)
│   ├── pages/
│   │   └── index.js              (KR 입력 폼)
│   ├── components/
│   │   ├── KRForm.jsx
│   │   ├── AIGeneration.jsx
│   │   └── KRRefine.jsx
│   ├── hooks/
│   │   └── useKRGeneration.js    (Claude API)
│   ├── data.json                 (R1의 mock 데이터)
│   └── README.md
│
├── r2-evaluator/                 👈 R2 담당 (완전 독립)
│   ├── pages/
│   │   ├── dashboard.js          (메인 대시보드)
│   │   └── approval.js           (승인 화면)
│   ├── components/
│   │   ├── Calendar.jsx
│   │   ├── StatsCard.jsx
│   │   ├── CoachingInput.jsx
│   │   └── AIValidation.jsx
│   ├── hooks/
│   │   └── useCoaching.js        (Claude API)
│   ├── data.json                 (R2의 mock 데이터)
│   └── README.md
│
├── r3-hr/                        👈 R3 담당 (완전 독립)
│   ├── pages/
│   │   ├── coaching-list.js      (사전 코칭 리스트)
│   │   ├── categories.js         (업무군 도출·확정)
│   │   └── dashboard.js          (분포 대시보드)
│   ├── components/
│   │   ├── RiskFilter.jsx
│   │   ├── CategoryManager.jsx
│   │   ├── DistributionChart.jsx
│   │   └── DetailPanel.jsx
│   ├── hooks/
│   │   └── useDiagnostics.js     (진단 엔진)
│   ├── data.json                 (R3의 mock 데이터)
│   └── README.md
│
├── shared/                       👈 공유 (공유 스키마 + 진단 엔진)
│   ├── types.js                  (공유 KR 데이터 스키마)
│   ├── diagnosticEngine.js       (위험 태깅 + 업무군 분류 룰)
│   ├── dataAccess.js             (MVP/시제품 추상화층)
│   ├── mockDataset.json          (전사 mock KR 데이터셋 10건)
│   └── README.md
│
└── docs/
    └── ARCHITECTURE.md           (역할별 폴더·데이터 흐름 문서)
```

**폴더 분리의 장점**:
- ✅ 각자 폴더만 건드림 → git 충돌 거의 없음
- ✅ 공유 파일(`shared/types.js`)은 한 건의 PR로 모두 검토
- ✅ 비동기 병렬 개발에 최적
- ✅ 3명이 동시에 개발할 때 40분 내 완료 가능 (병렬)

### 6.2 기술 스택 상세

```
프레임워크:  Next.js 14 (App Router) — 3역할 공통
             라우팅: /r1-employee (R1), /r2-evaluator (R2), /r3-hr (R3), /shared (공유)
스타일링:    Tailwind CSS
차트:        Recharts (R3 업무군 분포 · 난이도 분포 · 부서 편차)
언어:        JavaScript (JSX) — 프로토타입, TypeScript 미사용
상태관리:    React useState / useContext
데이터:      공유 KR 데이터셋(JSON) + 엑셀 업로드(SheetJS, R3)
             R1 가이드·예시는 정적 JS 모듈
진단 엔진:   ruleset 기반(결정적) JS 모듈 — 위험 태깅 + 업무군 1차 분류
             shared/diagnosticEngine.js로 R2·R3 중복 구현 방지
AI:          Anthropic Claude API (claude-sonnet-4-6 권장)
             — R1은 KR 초안 생성·대화형 정제·검토에 **상시 사용**(LLM 의존)
             — R2 코칭 문구 제안, R3 코칭 문구·새 업무군 후보 제안은 선택
             (R2·R3 핵심 진단은 룰 기반이라 API 없이 동작)
             폐쇄망 이관 시 사내 LLM 대체 필요. 로컬 시연은 인터넷 Claude API 사용
구동·배포:   시연 = 로컬 서버(npm run dev). AXHub 가용 시 이관
환경변수:    .env.local (ANTHROPIC_API_KEY) — .gitignore 필수
```

### 6.3 공유 데이터 계약 (KRSchema)

**shared/types.js에서 한 번에 정의**:
```javascript
export const KRSchema = {
  id: 'UUID',
  createdByRole: 'R1|R2|R3',
  status: 'draft|pending|approved|published',
  
  // R1이 입력 (변경 금지)
  title: 'string',
  description: 'string',
  measurable: boolean,
  challenging: boolean,
  objective: 'string|null',
  
  // R2가 추가/수정
  approvedAt: 'ISO timestamp | null',
  approver: 'string | null',
  violations: [{ rule: 'string', message: 'string' }],
  status: 'draft|pending|approved|rejected',
  
  // 공유 진단 엔진이 추가 (R2·R3 모두 사용)
  diagnosticTags: ['tag1', 'tag2'],  // 위험 태그 (코칭 후보)
  riskLevel: 'high|medium|low',
  category: 'development|data|automation|...', // 업무군
};
```

**공유 약속**:
- 스키마 수정 시: PR 열고 3명 모두 Approve 필수
- R1은 자신의 필드만 채우고 다른 역할 필드는 건드리지 않음
- R2·R3는 KR 수정 시 R1의 필드를 보존

---

## 7. 개발 우선순위 & 일정 (3주 병렬)

**총 일정: 3주 (21일) 병렬 개발**  
**개발 시간: 1대 PC 순차 ~3시간 vs 3대 PC 병렬 ~40분 (실제 작업)**

### 7.1 Day 1 오전 (2시간): 사전 준비 — 3명 함께

```
1. 프로젝트 초기화 (package.json, next.config.js, .gitignore)
2. shared/ 폴더 한 번에 생성 (Claude Code)
   - shared/types.js (공유 KR 스키마)
   - shared/diagnosticEngine.js (위험 태깅 + 업무군 분류)
   - shared/dataAccess.js (MVP/시제품 추상화층)
   - shared/mockDataset.json (mock KR 10건)
3. 3명이 types.js, diagnosticEngine.js 검토·동의
4. git commit & push
```

### 7.2 Day 1 오후: 각자 역할 폴더 생성 (병렬)

**R1 담당자**: r1-employee/ 폴더 구조 생성 + PR  
**R2 담당자**: r2-evaluator/ 폴더 구조 생성 + PR  
**R3 담당자**: r3-hr/ 폴더 구조 생성 + PR  

### 7.3 Day 1-2: 1차 개발 (병렬, 각자 폴더에서)

| 시간 | R1 | R2 | R3 |
|------|----|----|-----|
| **Day 1** | KR 입력 폼 UI | 대시보드 레이아웃 (캘린더, 통계) | 사전 코칭 리스트 UI |
| | Claude API 연동 시작 | Mock 데이터 | 진단 엔진 연동 |
| | Mock 데이터 준비 | PR 올리기 | PR 올리기 |
| | PR 올리기 | | |
| **Day 2 오전** | 대화형 정제 UI | KR 아코디언 뷰 | 업무군 도출 UI |
| | 정제 로직 구현 | AI Validation 버튼 | 분포 차트 기초 |
| | PR 올리기 | 승인/반려 버튼 | PR 올리기 |
| | | PR 올리기 | |
| **Day 2 오후** | ✅ 최소 경로 검증 | ✅ 최소 경로 검증 | ✅ 최소 경로 검증 |
| | (30분 동기) | (30분 동기) | (30분 동기) |

### 7.4 Day 2 오후: 최소 경로 통합 검증 (30분 동기)

**목표**: R1→R2→R3까지 최소 1건의 KR이 완전히 흐르는 것 확인

```
1️⃣ R1 (5분): shared/mockDataset.json에 테스트 KR 1건 추가
   "로그인 응답 시간 50% 단축", measurable=true, challenging=true

2️⃣ R2 (5분): 위 KR 조회 → AI Validation 실행 → 승인
   결과: "규칙 검증 완료, 위반 없음"

3️⃣ R3 (5분): 위 KR 조회 → 업무군 자동 태깅 → 분포에 반영
   결과: "개발/요건 1건 추가됨" 확인

4️⃣ 검증 (10분): 스키마 맞나? 진단 엔진 일관성 있나?
```

**발견 시 조치**:
- 스키마 오류 → 즉시 shared/types.js 수정 (3명 동의)
- 진단 엔진 오류 → shared/diagnosticEngine.js 수정 (R2·R3 검증)

### 7.5 Day 3-14: 2차 개발 (병렬, 공유 스키마 확정 후)

| 역할 | Feature | 목표 |
|------|---------|------|
| **R1** | Feature 3 (최종 검토) | 단일 모델/룰 기반 검토 → 확정·제출 |
| | Feature 고도화 | 자동 정제 AI 연동 |
| | 테스트 | 10개 KR 생성 및 제출 확인 |
| **R2** | Feature 3 (OKR 검토/승인) | AI Validation 강화, 승인 로직 완성 |
| | Feature 1 확장 | 캘린더 + 현황 카드 완성 |
| | Feature 2 강화 | 코칭 기록 + AI 제안 Confirm |
| **R3** | Feature 2 (업무군 도출·확정) | 1차 분류 + 인사담당자 확정 UI |
| | Feature 3 (선택) | 난이도·품질 2축 분포 (시간 부족 시 생략) |
| | 테스트 | 10개 이상 KR 분석 및 태깅 |

### 7.6 Day 15-19: 통합 테스트 + 핸드오프 검증

```
매일 (Daily):
- R1: 10개 이상 KR 작성 (매일 2-3개씩)
- R2: 위 KR 승인 (매일)
- R3: 분포 및 위험도 확인 (매일)

금요일 (통합 검증):
- "데이터 흐름이 자연스러운가?"
- "R1→R2→R3 과정에서 데이터 손실 없나?"
- "3주를 통해 100건 이상 KR 처리 가능한가?"
```

### 7.7 Day 20-21: 마지막 안정화 + 시연 리허설

```
- UI 디버깅 및 버그 수정
- 시연 시나리오 리허설 (10분 demo)
  1. R1: "직원이 KR 입력 → 초안 생성 → 정제 → 제출" (2분)
  2. R2: "평가자가 KR 조회 → AI Validation → 승인" (2분)
  3. R3: "인사담당자가 전사 현황 조회 → 위험 KR 식별 → 분포 분석" (3분)
  4. 통합: R1 KR이 R3 대시보드에 반영된 모습 (3분)
- 비상 계획 (git 충돌·데이터 손실 시 롤백)
```

### 7.8 협력 프로세스 (비동기 팀용)

**매일**:
- 저녁: 각자 진도를 PR로 올림 (Feature 단위)
- 다음 아침: 3명 중 1명이 리뷰·승인

**주 2회 동기 (30분)**:
- **수요일**: 진도 공유 + 블로커 해결 (5-10분 회의)
- **금요일**: 주간 통합 검증 + 다음 주 계획 (15-20분 회의)

**Slack/노션**:
- 스키마 변경: #design (3명 동의 필수)
- 기술 블로커: #dev-alert (긴급 처리)
- 변경 내역: 노션 "Data Contract" (비동기 기록)

---

## 8. 데이터 저장소 & 고도화 로드맵

### 8.1 MVP (3주) — 파일 기반

**데이터 저장**:
- R1: `r1-employee/data.json` (작성 중인 KR)
- R2: `r2-evaluator/data.json` (승인 기록)
- R3: `r3-hr/data.json` (업무군 확정 기록)
- 공유: `shared/mockDataset.json` (전사 KR 스냅샷)

**추상화층** (MVP와 시제품 모두 호환):
```javascript
// shared/dataAccess.js
export async function getKRData(filters = {}) {
  if (process.env.USE_DB === 'true') {
    return await fetchFromDatabase(filters);
  } else {
    return loadFromJSON('./data.json', filters);
  }
}

export async function saveKRData(kr) {
  if (process.env.USE_DB === 'true') {
    return await saveToDatabase(kr);
  } else {
    return saveToJSON('./data.json', kr);
  }
}
```

**보안**: 더미 데이터 사용, `.gitignore`에 `data.json` 포함

### 8.2 시제품 (4주차 이후) — DB 기반 고도화

**필요 공수**: 약 1주 반 (26~35시간)

| 작업 | 공수 | 설명 |
|------|------|------|
| 1. DB 스키마 설계 | 4~6h | KR 데이터 + 코칭 기록 + 업무군 테이블 |
| 2. API 레이어 작성 | 6~8h | CRUD 엔드포인트(R1/R2/R3용) + 인증 |
| 3. 데이터 마이그레이션 | 2~3h | data.json → DB 스크립트 |
| 4. 각 역할 코드 수정 | 10~12h | `data.json` 호출 → API 호출로 변경 |
| 5. 테스트 + 검증 | 4~6h | API 테스트 + 데이터 무결성 확인 |
| **총 공수** | **26~35시간** | 약 1주 반 |

---

## 9. 팀 협력 가이드

### 9.1 Git 워크플로우

**브랜칭 전략**:
```
main (배포·시연 기준)
  ├─ feature/r1-kr-generation (R1 담당)
  ├─ feature/r2-validation (R2 담당)
  └─ feature/r3-diagnostic (R3 담당)
```

**PR & 리뷰**:
- 각자 feature 브랜치에서 개발
- 매일 저녁: PR 올림 (Feature 단위)
- 매일 아침: 3명 중 1명이 리뷰·승인
- main 병합

**공유 파일 변경**:
- `shared/types.js`, `shared/diagnosticEngine.js` 수정 시:
  - PR 열고 3명 모두 Approve 필수
  - 진도 회의에서 함께 검증

### 9.2 코드 리뷰 포인트

- 공유 스키마 준수 여부
- 진단 엔진 로직 일관성 (R2·R3)
- 데이터 흐름 정합성 (각 역할의 입출력)

### 9.3 진도 동기화 (주 2회, 30분)

**수요일**: 진도 + 블로커
- 각자 어디까지 했는가 (Feature 기준)
- 막힌 부분 있는가 (기술·스키마)
- 내일 우선순위

**금요일**: 통합 검증 + 다음 주 계획
- 이번 주 구현 기능 테스트
- R1→R2→R3 데이터 흐름 확인
- 다음 주 Feature 목표

### 9.4 긴급 소통

**Slack #dev-alert**: 기술 블로커 (목표: 1시간 내 해결)  
**Slack #design**: 데이터 계약 변경 (3명 동의 후)  
**노션**: 변경 이력 기록 (Data Contract, Diagnostic Rules)

---

## 10. 참고 문서

- **DEVELOPMENT_ROADMAP.md**: 각 팀원별 상세 개발 순서 (Day별, Feature별)
- **FEEDBACK_INTEGRATED.md**: 피드백 분석 및 개선 전략
- **spec.md**: 원본 v1.4 (기존 유지)

---

## 변경 이력

| 버전 | 날짜 | 내용 |
|------|------|------|
| 통합 v1.0 | 2026-06-22 | R1·R2·R3 spec 통합 |
| 통합 v1.1 | 2026-06-22 | Next.js 14 기술 스택 통일 |
| 통합 v1.2 | 2026-06-22 | R1 위저드 프로세스 재설계, 멀티 AI → 단일 모델로 축소 |
| 통합 v1.3 | 2026-06-22 | 정렬성 제외, 상위 Objective 선택 입력 |
| 통합 v1.4 | 2026-06-22 | R1 품질 진단 화면 제외 |
| **최종 v2.0** | **2026-06-24** | **B모드 완전 통합** — 폴더 구조·데이터 계약·일정 재구성·팀 협력 가이드·DB 로드맵 추가. 3주 병렬 개발 완전 문서화. 팀원별 상세 로드맵(DEVELOPMENT_ROADMAP.md) 동반 |

---

## 📌 **최종 요약**

| 항목 | 상태 |
|------|------|
| **일정** | 3주 (Day 1-21) 병렬 개발 ✅ |
| **폴더 구조** | r1/, r2/, r3/, shared/ 완전 분리 ✅ |
| **공유 계약** | types.js + diagnosticEngine.js 명시 ✅ |
| **병렬 개발 시간** | 1대 PC 순차 3시간 vs 3대 PC 병렬 40분 ✅ |
| **최소 경로 검증** | Day 2 오후 (스키마 조기 발견) ✅ |
| **협력 프로세스** | 일일 PR + 주 2회 동기 미팅 ✅ |
| **DB 마이그레이션** | 시제품 4주차 이후 (26~35시간) ✅ |
| **팀원별 가이드** | DEVELOPMENT_ROADMAP.md 별도 제공 ✅ |

**이 문서(new_spec.md)는 spec.md v1.4 + 모든 B모드 개선사항을 완전히 통합한 최종 기획 문서입니다.**  
**3명의 팀원이 이 문서와 DEVELOPMENT_ROADMAP.md만으로 바로 개발을 시작할 수 있습니다.**
